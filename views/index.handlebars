<h1> BURGERS! </h1>

<h2>Burgers I Will Eat</h2>

<ul>
    {{#each burgers}}
        {{#unless eaten}}
            <li>
                {{burger_name}}
                <button class="eatMe" data-id="{{id}}">Ate It!</button>
            </li>
        {{/unless}}
    {{/each}}

</ul>

<h2>Burgers I Have Eaten</h2>

<ul>
    {{#each burgers}}
        {{#if eaten}}
            <li>{{burger_name}}</li>
        {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca"> Burger Name:</label>
        <input type="text" id = "ca" name="name">
    </div>

    <button type="submit">Press to Add!</button>
</form>